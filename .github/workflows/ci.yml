name: Demo CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Build
        run: echo "Building project..."

      - name: Test
        run: echo "Running tests..."

      - name: Deploy
        run: echo "Deploy successfu!"